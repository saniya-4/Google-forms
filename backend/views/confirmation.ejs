<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Form Response</title>
  <style>
    body {
      background: #f3eafa;
      font-family: 'Google Sans', Arial, sans-serif;
      margin: 0;
      padding: 0;
    }
    .confirm-box {
      background: #fff;
      max-width: 650px;
      margin: 32px auto;
      padding: 32px 32px 28px 32px;
      border-radius: 10px 10px 10px 10px;
      box-shadow: 0 1px 4px rgba(60,60,60,0.10);
      border-top: 8px solid #673ab7;
    }
    .confirm-box h2 {
      font-size: 2rem;
      font-weight: 600;
      margin: 0 0 8px 0;
      color: #222;
      font-family: inherit;
    }
    .confirm-box div {
      margin-bottom: 22px;
      font-size: 1.14rem;
      color: #333;
    }
    .confirm-link {
      color: #1967d2;
      text-decoration: underline;
      font-size: 1rem;
      cursor: pointer;
    }
    @media (max-width: 700px) {
      .confirm-box {
        margin: 16px;
        padding: 20px 8px;
      }
    }
  </style>
</head>
<body> 
  <div class="confirm-box">
    <h2>Google Form</h2>
    <div>Your response has been recorded.</div>
    <a class="confirm-link" href="/">Submit another response</a>
  </div>
</body>
</html>
